# Legate native references are Python (cunumeric). Nothing to compile; the top-level
# run script invokes $(LEGATE_PYTHON) on each task/main.py.

.PHONY: all
all:
	@echo "Legate: no compile step — use scripts/run_native_references.sh"
