# Heat-propagation benchmark data

The default heat-propagation benchmark generates synthetic initial plates in-memory (ambient field + deterministic hotspots), so no input dataset is required.

If you want fixed domain-specific temperature maps, place them here and document how they are loaded.
